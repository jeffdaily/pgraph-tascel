#ifndef LIB_H_
#define LIB_H_

/** int power function */
int power(int base, int n);

/** size_t power function */
size_t zpower(size_t base, size_t n);

int leaf_key_cmp(const void *vp1, const void *vp2);

#endif /* end of lib.h */
